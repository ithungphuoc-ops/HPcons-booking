# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# bản thử nghiệm cũ (Express + SQLite / Vite) trước khi chuyển sang Next.js hiện tại — không
# liên quan tới app đang chạy thật, chưa từng commit, chặn hẳn để tránh lỡ tay cuốn theo git add -A
/backend/
/frontend/
/reset.bat
/start.bat

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel
.env*
